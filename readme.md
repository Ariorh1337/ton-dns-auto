# Wild Auto DNS

Это умный контракт для TON, который делает управление DNS-записями простым и автоматическим. Он позволяет создавать поддомены и управлять ими без необходимости вручную настраивать каждый поддомен.

## 🎯 В чем отличие от обычного DNS?

1. **Автоматическое наследование**:
   - В обычном DNS каждый поддомен нужно настраивать отдельно
   - В Wild Auto DNS поддомены автоматически наследуют настройки от родительского домена
   - Например, если у вас есть домен `ariorh.ton` и поддомен `test.ariorh.ton`, то `test.ariorh.ton` автоматически получит все настройки от `ariorh.ton`

2. **Простота управления**:
   - Не нужно создавать отдельные контракты для каждого поддомена
   - Все поддомены управляются через один контракт
   - Можно быстро добавлять и изменять записи

## 🚀 Как это работает?

1. **Важное замечание о доменах**:
   - Корневой домен (например, `ariorh.ton`) управляется через ADNL (Abstract Datagram Network Layer)
   - Поддомены (например, `test.ariorh.ton`) управляются через DNS-контракт
   - Контракт работает только с поддоменами, не затрагивая корневой домен

2. **Создание контракта**:
   - Контракт создается для управления поддоменами
   - Владелец контракта может добавлять DNS-записи для поддоменов

3. **Работа с поддоменами**:
   - Когда кто-то запрашивает поддомен (например, `test.ariorh.ton`), контракт:
     - Сначала проверяет, есть ли специальные настройки для `test.ariorh.ton`
     - Если нет, автоматически использует настройки от `ariorh.ton`
     - Это позволяет не создавать отдельные записи для каждого поддомена
	- Как пример `ariorh.ton` содержит adnl, когда кто то запрашивает не существующий домен `sub-domain.ariorh.ton` то получит adnl родительского `ariorh.ton`

4. **Управление записями**:
   - Владелец может добавлять, изменять и удалять DNS-записи для поддоменов
   - Можно создавать специальные настройки для конкретных поддоменов
   - Все изменения мгновенно применяются

## 🛠️ Полная инструкция по настройке

### 1. Подготовка и деплой

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Заполните переменные в `.env`:
   ```
   TON_ENDPOINT=https://toncenter.com/api/v2/jsonRPC
   TON_API_KEY=your_api_key_here
   MNEMONIC="your twelve word mnemonic phrase here"
   DOMAIN=ariorh.ton
   DNS_AUTO_CONTRACT_ADDRESS="EQ..." # Будет автоматически заполнено после деплоя
   ```

3. Соберите и деплойте контракт:
   ```bash
   npm run build
   npm run deploy
   npm run update -- <домен> <категория> <значение>
   ```

### 2. Настройка DNS записей

После деплоя нужно настроить DNS записи в правильном порядке:

1. **Настройка в dns.ton.org**:
   - Добавьте ADNL запись для основного домена `ariorh.ton`
   - Добавьте Smart contract запись (категория 0) с адресом вашего контракта
   - Это нужно для того, чтобы TON знал, что ваш контракт управляет субдоменами

2. **Настройка в контракте**:
   ```bash
   # Добавляем запись для основного домена
   npm run update -- ariorh.ton 0 "0x..." # ADNL адрес для ariorh.ton
   ```
   Это нужно потому что:
   - Контракт хранит все записи в своем хранилище `records`
   - При запросе субдомена, контракт сначала ищет запись для него
   - Если не находит, ищет запись для родительского домена
   - Без записи для `ariorh.ton` контракт не знает, что возвращать для субдоменов

3. **Добавление субдоменов**:
   ```bash
   # Добавить ADNL для субдомена
   npm run update -- test.ariorh.ton 0 "0x..." # ADNL для test.ariorh.ton
   
   # Или добавить сайт
   npm run update -- blog.ariorh.ton 2 "https://blog.ariorh.ton"
   ```

### 3. Как это работает

1. **Для основного домена**:
   - `ariorh.ton` → открывается через ADNL из `dns.ton.org`
   - Контракт не влияет на основной домен

2. **Для субдоменов**:
   - Контракт сначала ищет запись для субдомена
   - Если не находит, использует запись для `ariorh.ton`
   - Это позволяет наследовать настройки от основного домена

3. **Хранение записей**:
   - Все записи хранятся в `records` контракта
   - Каждая запись имеет категорию и значение
   - Контракт не имеет доступа к записям из `dns.ton.org`

### 4. Пример полной настройки

```bash
# 1. Деплой контракта
npm run deploy

# 2. Добавление записи для основного домена
npm run update -- ariorh.ton 0 "0x..." # ADNL для ariorh.ton

# 3. Добавление субдоменов
npm run update -- test.ariorh.ton 0 "0x..." # ADNL для test.ariorh.ton
npm run update -- blog.ariorh.ton 2 "https://blog.ariorh.ton"
```

После этого:
- `ariorh.ton` → открывается через ADNL из `dns.ton.org`
- `test.ariorh.ton` → открывается через ADNL из контракта
- `blog.ariorh.ton` → открывается как сайт
- Другие субдомены → наследуют ADNL от `ariorh.ton`

## 🛠️ Как использовать?

### 1. Настройка окружения

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Заполните переменные в `.env`:
   ```
   TON_ENDPOINT=https://toncenter.com/api/v2/jsonRPC
   TON_API_KEY=your_api_key_here
   MNEMONIC="your twelve word mnemonic phrase here"
   DOMAIN=test.ton
   DNS_AUTO_CONTRACT_ADDRESS="EQ..." # Будет автоматически заполнено после деплоя
   ```

### 2. Разница между update_record и register_subdomain

В контракте есть два основных метода для работы с поддоменами:

1. **update_record**:
   - Добавляет или обновляет DNS записи для домена/поддомена
   - Не создает новый поддомен
   - Используется для настройки ADNL, сайтов, кошельков и т.д.
   - Пример:
     ```bash
     # Добавить ADNL запись
     npm run update -- ariorh.ton 0 "EQ..."
     
     # Добавить сайт
     npm run update -- blog.ariorh.ton 2 "blog.ariorh.ton"
     ```

2. **register_subdomain**:
   - Создает новый поддомен и назначает ему владельца
   - Используется для делегирования управления поддоменом другому контракту
   - После регистрации поддоменом управляет только назначенный владелец
   - Пример:
     ```bash
     # Создать поддомен shop.ariorh.ton и назначить ему владельца
     npm run register -- shop.ariorh.ton EQ... # адрес контракта-владельца
     ```

#### Когда что использовать?

- Если вы хотите просто добавить DNS запись (ADNL, сайт и т.д.) → используйте `update_record`
- Если вы хотите создать независимый поддомен с отдельным управлением → используйте `register_subdomain`

### 3. Развертывание контракта

1. Соберите контракт:
   ```bash
   npm run build
   ```
2. Разверните контракт:
   ```bash
   npm run deploy
   ```
   После успешного деплоя:
   - Адрес контракта автоматически сохранится в файл `.env`
   - В консоли вы увидите сообщение об успешном развертывании

### 4. Управление записями

#### Категории DNS записей

В TON DNS используются следующие категории записей:
- `0`: Смарт-контракт
- `1`: Адрес кошелька
- `2`: Сайт
- `3`: Хранилище
- `4`: DNS-сервер
- `5`: SMTP-сервер
- `6`: Jabber-сервер
- `7`: Telegram-бот
- `8`: Telegram-канал
- `9`: Telegram-группа
- `10`: Telegram-пользователь
- `11`: IPFS-хэш
- `12`: Tor-адрес
- `13`: I2P-адрес
- `14`: Freenet-адрес
- `15`: ZeroNet-адрес
- `16`: Onion-адрес
- `17`: Onion3-адрес
- `18`: IP-адрес
- `19`: IPv6-адрес
- `20-31`: Дополнительные DNS-записи

#### Время жизни записей

- Записи не имеют явного времени жизни
- Они существуют до тех пор, пока:
  - Контракт не будет закрыт
  - Запись не будет перезаписана новой
  - Владелец не изменит запись

#### Добавление записей

Добавление записи: (примеры использования смотри ниже в главе #Примеры использования)

```bash
npm run update -- <домен> <категория> <значение>
```

### 5. Закрытие контракта

Для закрытия контракта и возврата средств:
```bash
npm run terminate
```

## 📝 Примеры использования

### Создание поддоменов
```bash
# Поддомен для смарт-контракта
npm run update -- contract.ariorh.ton 0 "EQCD39VS5jcptHL8vMjEXrzGaRcCVYto7HUn4bpAOg8xqB2N"

# Поддомен для ADNL
npm run update -- adnl.ariorh.ton 0 "0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef"

# Поддомен для кошелька
npm run update -- wallet.ariorh.ton 1 "EQCD39VS5jcptHL8vMjEXrzGaRcCVYto7HUn4bpAOg8xqB2N"

# Поддомен для сайта HTTPS
npm run update -- blog.ariorh.ton 2 "https://blog.ariorh.com"
# Поддомен для сайта HTTP
npm run update -- shop.ariorh.ton 2 "http://shop.ariorh.com"
npm run update -- store.ariorh.ton 2 "store.ariorh.com"

# Поддомен для Telegram бота
npm run update -- bot.ariorh.ton 7 "@my_bot"
npm run update -- channel.ariorh.ton 8 "@my_channel"
npm run update -- group.ariorh.ton 9 "@my_group"

# Поддомен для IPFS контента
npm run update -- ipfs.ariorh.ton 11 "QmXoypizjW3WknFiJnKLwHCnL72vedxjQkDDP1mXWo6uco"

# Поддомен для Tor скрытого сервиса
npm run update -- tor.ariorh.ton 12 "abcdefghijklmnop.onion"
npm run update -- tor.ariorh.ton 17 "abcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyz.onion"

# Поддомен для API
npm run update -- api.ariorh.ton 18 "192.168.1.1"
npm run update -- api.ariorh.ton 19 "2001:0db8:85a3:0000:0000:8a2e:0370:7334"
```

### Автоматическое наследование
- Если запросить `sub.test.ariorh.ton`:
  - Сначала проверяется наличие специальных настроек для `sub.test.ariorh.ton`
  - Если нет, используются настройки от `test.ariorh.ton`
  - Если и их нет, используются настройки от `ariorh.ton`

## 🔒 Безопасность

- Только владелец контракта может изменять DNS-записи
- Все транзакции подписываются приватным ключом
- Контракт можно закрыть и вернуть средства в любой момент

## 🌟 Почему это удобно?

### 1. Аналогия с Wildcard DNS
- В обычном DNS для создания поддоменов нужно:
  - Добавлять отдельные A-записи для каждого поддомена
  - Или использовать Wildcard DNS (`*.ariorh.com`)
  - Но Wildcard DNS не позволяет настраивать отдельные поддомены

- В Wild Auto DNS:
  - Все поддомены автоматически наследуют настройки
  - Можно переопределить настройки для любого поддомена
  - Не нужно заранее создавать записи для всех поддоменов

### 3. Преимущества перед другими решениями

1. **Автоматическое наследование**:
   - Поддомены автоматически получают настройки родительского домена
   - Не нужно создавать отдельные записи для каждого поддомена
   - Можно переопределить настройки для любого поддомена

2. **Гибкость настройки**:
   - Можно быстро добавлять новые поддомены
   - Можно изменять настройки существующих поддоменов
   - Можно удалять поддомены

3. **Простота управления**:
   - Все поддомены управляются через один контракт
   - Не нужно создавать отдельные контракты для каждого поддомена
   - Можно быстро добавлять и изменять записи

4. **Безопасность**:
   - Только владелец контракта может изменять настройки
   - Контракт можно закрыть в любой момент

### 4. Как это работает технически

1. **Структура контракта**:
   - Контракт хранит настройки для всех поддоменов
   - Каждый поддомен может иметь свои настройки
   - Если поддомен не имеет своих настроек, используются настройки родительского домена

2. **Процесс запроса**:
   - Когда кто-то запрашивает поддомен, контракт:
     1. Проверяет наличие настроек для запрошенного поддомена
     2. Если настройки есть, возвращает их
     3. Если настроек нет, проверяет родительский домен
     4. Если и там нет настроек, возвращает настройки корневого домена

3. **Пример работы**:
   ```
   Запрос: sub.test.ariorh.ton
   1. Проверяем sub.test.ariorh.ton -> нет настроек
   2. Проверяем test.ariorh.ton -> нет настроек
   3. Возвращаем настройки ariorh.ton
   ```

## 🌐 Интеграция с Nginx

### 1. Настройка маршрутизации поддоменов

Контракт Wild Auto DNS идеально подходит для работы с Nginx в качестве системы маршрутизации поддоменов. Вот как это работает:

1. **Основная конфигурация сервера**:
   ```nginx
   # /etc/nginx/sites-available/ariorh.ton
   server {
       listen 80;
       listen 443 ssl;

       server_name _;

       ssl_certificate /etc/nginx/ssl/self.crt;
       ssl_certificate_key /etc/nginx/ssl/self.key;

       return 301;
   }
   ```

2. **Конфигурация маршрутизации**:
   ```nginx
   # /etc/nginx/backend-map.conf
   # Убираем порт из $host (например, :443)
   map $host $clean_host {
       default "";
       ~^(?<host_only>[^:]+) $host_only;
   }

   # Поддомен → порт
   map $clean_host $backend_port {
       default 8080;
       test1.ariorh.ton 1802;
       test2.ariorh.ton 2222;
       test3.ariorh.ton 3001;
       test4.ariorh.ton 1803;
       test5.ariorh.ton 3000;
   }
   ```

3. **Настройка основного конфига Nginx**:
   ```nginx
   # /etc/nginx/nginx.conf
   # server_names_hash_bucket_size 64;
   # server_name_in_redirect off;

   include /etc/nginx/mime.types;
   default_type application/octet-stream;

   # ТУТ МЫ МАПИМ ПОРТЫ С СУБДОМЕНАМИ
   include /etc/nginx/backend-map.conf;

   ##
   # SSL Settings
   ##

   ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3; # Dropping SSLv3, ref: POODLE
   ssl_prefer_server_ciphers on;
   ```

4. **Процесс добавления нового поддомена**:
   ```bash
   # 1. Добавляем запись в DNS
   npm run update -- test6.ariorh.ton 2 "test6.ariorh.ton"

   # 2. Добавляем конфигурацию в Nginx
   # Редактируем /etc/nginx/backend-map.conf
   map $clean_host $backend_port {
       # ... существующие записи ...
       test6.ariorh.ton 3002;
   }

   # 3. Перезагружаем Nginx
   sudo nginx -t        # Проверяем конфигурацию
   sudo nginx -s reload # Перезагружаем Nginx
   ```

5. **Важные замечания**:
   - После добавления нового поддомена необходимо перезагрузить Nginx
   - Всегда проверяйте конфигурацию перед перезагрузкой
   - Используйте `nginx -t` для проверки синтаксиса
   - Используйте `nginx -s reload` для безопасной перезагрузки
   - Убедитесь, что файл `backend-map.conf` включен в основной конфиг Nginx

6. **Преимущество использования одного ADNL**:
   - Не нужно запускать отдельный ADNL для каждого поддомена
   - Все поддомены автоматически наследуют ADNL родительского домена
   - Экономия ресурсов и упрощение управления
   - Легкость масштабирования - можно добавлять новые поддомены без настройки дополнительных ADNL

## 🤝 Лицензия

MIT